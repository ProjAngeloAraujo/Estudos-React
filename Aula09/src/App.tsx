import { AuthForm } from "./components/form"

function App() {
  return (
    <main className="min-h-screen flex items-center justify-center p-4 bg-gradient-to-br from-blue-50 via-cyan-50 to-teal-50">
      <AuthForm />
    </main>
  )
}

export default App
